// These are the Codex choices NanoDuck can offer. A connected account must
// also advertise a model/effort pair before it appears in Settings.
export const codexModelEfforts = Object.freeze({
  "gpt-6-astra": Object.freeze(["xhigh", "ultra"]),
  "gpt-6-sol": Object.freeze(["low", "medium", "high", "xhigh", "max", "ultra"])
});
