# Definition of done and evaluation gates

## Source references and verification profile

Current PRD, context/terms, guardrails, journey, screen map, wireframes, design brief and architecture define this profile. This document defines gates; it does not execute tests. **Definition status: prepared. Release readiness: not_evaluated.** Visual binding is `nanoduck-electric-a-v8-20260914`, using the target/tree hashes and permitted variance in the design brief. Existing QA memberships remain prepared and will be rebound by the QA owner in the following pass.

## Definition of done model

Acceptance means an observable requirement holds at its declared seam. Completion additionally requires current source/baseline binding, appropriate evidence, no open blocking findings, protected data and exact authorized scope. A design candidate can pass a local simulation check while production behavior remains undeployed and unverified. Local implementation checks are evidence only for their named seam.

## Gate matrix

| Gate | Purpose and applicable source | Required evidence and pass/block rule | Rerun and automation |
|---|---|---|---|
| product_functional_requirements | PRD functional clauses, all UCs, complete surface/state coverage and preserved models. | Actual browser/service/provider results for production; separately labelled simulation evidence for design checks. For a substantive consultation, evidence must show Head → each selected specialist task, each specialist → Critic, the selected number of Critic ↔ specialist exchanges for every selected specialist, then each specialist → Head final position, Critic → Head assessment of all final positions, and one Head → owner Consolidated advice. Verify that delaying or failing any closing contribution prevents Head synthesis, restart preserves committed closing messages, and Critic disagreement prevents early Auto completion. Outcome must not show a Head assignment as final advice. Missing even one selected specialist review blocks completion. Verify fixed-depth coverage for all supported team sizes, whole-team Auto agreement and its per-specialist cap, interrupted review replay, and provider failure before the final missing reply; Head must not synthesize an incomplete review. Pre-conclusion Head advice or specialist-to-specialist routing blocks. Test that malformed or generic pre-conclusion Head prose is never committed, receives one corrective retry, and only then falls back to a bounded case-specific task containing the stated decision. Every accepted task must repeat both an owner-case anchor and another decision detail; test explicit recipient binding for every specialist task. Test that every visible contribution remains inside its role bound at a completed sentence and does not contain unsupported invented figures, market claims, customer behavior or sources. Every applicable clause must have a concrete check; false success, missing required flow or silent model substitution blocks. For preferences, prove that fresh and pre-Opus settings rows receive Claude Code `claude-opus-5-5` / `medium` exactly once while unrelated preferences survive; a row from the immediately preceding revision with Opus 5.5 / Low in either Claude branch advances only that effort to Medium once; later supported Codex and other Claude selections survive repeated restart; and an already accepted run keeps its original snapshot. At the Claude process seam prove the installed CLI matches the pinned v2.1.280, Opus 5.5 becomes `--model claude-opus-5-5 --effort medium` without a moving alias, Extra becomes `xhigh`, and a legacy `claude-opus-5` snapshot passes its exact model ID and fails visibly if unsupported. A live allowed Opus 5.5 turn remains a separate release check. | Changed behavior, provider/catalog or state mapping; automation not available yet for production, manual simulation review available. |
| product_security_requirements | All PRD security clauses listed below, across their applicable UCs. | Positive and denied/adversarial checks at the actual enforcement boundary, configuration/dependency and protected-data evidence. Verify permitted Markdown renders as content while raw HTML, unsafe links and prohibited hosts remain inert or rejected. For Claude Critic, prove command-level tool denial, no direct research and rejection before persistence of an internal tool transcript, including its one bounded text-only retry and safe failure after a second trace. All required obligations must pass; a mockup or screenshot cannot satisfy this gate. | Changed trust/data/input/identity/provider/hosting boundary; implementation test runner and authorized manual review, not available until implementation exists. |
| approved_visual_baseline_fidelity | Whole selected candidate, its scope/targets and NFR-02.1–02.3 / FR-08.1–08.3. | Current Baseline ID, frozen target/tree hashes, route/state/viewport, permitted variance and visual evidence with no unexplained drift. Before selection this is a parameterized definition, not an approval claim. | Any baseline/source/rendering change; browser/visual review plus later comparison tooling. |
| heuristic_usability_review | H1–H10 across J-01–J-07, seven UCs and every applicable state/surface. | Named expert review of desktop/mobile tasks and recovery, with evidence and classified findings. Critical omissions or open blocking findings block; screenshots alone do not prove the review. | Changed navigation, flow, copy, controls or state behavior; manual expert review. |
| representative_user_task_validation | Critical consultation, voice, Settings, interruption and record-control tasks from the design brief. | Observed representative-owner task completion with task/device/success criterion and findings. AI review is not user research. Required deferred/unrun tasks cannot pass release. | Material task/interaction change or resolved blocking finding; manual owner sessions, not yet run. |
| lifecycle_and_continuity | NFR-01.1–01.4 and architecture's deployment/restore/model-preservation boundary. | Actual acceptance/restart/Stop/retry evidence, isolated restore/deletion behavior, exact app/database ownership, runtime/configuration and post-deploy/rollback observations. Reload a scrolled authenticated S-02/S-03/S-04 view and prove the same browser tab restores the prior surface and reading position; S-02 also restores the local tab and protected record after reloading it. Inspect the short-lived tab state to prove it holds no draft/record content, never changes the URL and is removed at Logoff. Reopen a GoDaddy-backed saved record whose MySQL JSON fields are returned as decoded values, and prove its run snapshot, messages and sources render without a server error. Exercise a slow ephemeral provider turn and prove its completed message item is committed once only after matching successful terminal completion, without reading saved thread history. Cover early events, unrelated turn IDs, failed completion after a message item, cancellation and connection closure; restart during an unresolved turn and prove the database-backed run resumes from the last confirmed message. For an app-server JSON-RPC error, prove the protected log holds only the normalized recovery category, safe numeric code and failed operation, while the owner receives the corresponding recovery state and the saved question remains intact. On the named GoDaddy app, verify the protected Codex auth seed enters only the owned encrypted grant row, a refreshed grant survives another process start without being replaced by an unchanged seed, `auth.json` is materialized only in a private temporary app-server home, and the pinned package starts it. A hard kill between token rotation and durable write-back, or write-back failure after rotation, is an explicit recovery limit: a fresh trusted reseed may be necessary, and a healthy build or local fixture does not prove live Codex readiness. Repeat startup and configuration-recovery migration at the actual database seam: only the owner preference in `nanoduck_settings` may change, `nanoduck_runs` stays byte-equivalent, the revision is idempotent, and recovered old preferences affect future consultations only. No broad GoDaddy changes; absence of required host evidence blocks release. | State/storage/provider/runtime/deployment changes; production harness and scoped host inspection unavailable until later authorization. |

For FR-05.1 within `product_functional_requirements`, verify that a returning browser receives the current Settings controls after an app update without manual cache clearing. The Critic Claude Code branch must remain visible and show Opus 5.5 with Medium (default), High, Extra and Max; Low must be absent for that model. When managed Claude authentication is unavailable, those choices may be shown as a clearly labelled preview but cannot be saved or used for a turn; readiness must be verified separately. Also verify that GPT-6 Sol appears as a selectable option in both Codex model controls only with supported efforts from the current catalog. Selecting and saving it must preserve the other Codex branch, the current Astra/Opus defaults until an owner change, and every accepted run snapshot. An unavailable catalog or invalid model/effort pair cannot silently select another value. This defines required evidence; no new provider turn or formal release check is claimed here.

For FR-05.1–05.2, prove that an owner can save future Settings during an active Codex turn without changing that run's accepted provider/model/effort snapshot. A fresh catalog may be read through the active app-server connection without opening a second grant stream; when that read fails, Settings may retain only the previously saved Codex pairs and permit unrelated preference saves. A new or crossed Codex pair then requires fresh catalog evidence and must fail visibly until available. Reopening Settings must not turn a transient busy or unavailable catalog into a claim of model entitlement. FR-05.5 separately requires selected-provider authorization failure, quota and outage to retain their distinct recovery actions.

For NFR-11.3 and NFR-14.2 within `product_security_requirements`, verify that current HTML remains no-store, its same-origin CSS/JavaScript URLs identify the bytes served (including imported modules), a wrong or stale hash cannot return different bytes, and no private response or draft is embedded in a public asset. Local asset tests are useful implementation evidence; the actual Preview cache and running revision remain separate host checks.

For NFR-11.2, NFR-13.1, NFR-14.1, NFR-15.1 and NFR-16.1–16.2 within `product_security_requirements`, verify the Codex grant at the actual persistence and process boundaries. A structurally invalid seed cannot replace a valid row; an unchanged seed cannot roll back a refreshed row; a fresh trusted credential may reseed it. Database ciphertext, authentication tag, separated key and tamper rejection must protect the grant; an owner recovery export, API response, browser asset and protected log must not reveal auth bytes or a usable grant. One database-backed leader must exclude a second process and maintain ownership while idle; generation fencing must reject stale write-back. During inspect, turn completion, Stop, provider shutdown and connection failure, any rotated file must be validated and committed before private-home cleanup; a failed read, decrypt or write-back fails closed without a partial consultation commit, paid fallback or silent model substitution. Test both the recoverable retry and the stale-grant case that needs a fresh trusted login; never claim that a hard-kill rotation window can be eliminated by application tests alone. A separate real-host check must establish the deployed grant and provider behavior without exposing its value.

### Product security membership

The `product_security_requirements` gate covers NFR-10.1, NFR-10.2, NFR-10.3, NFR-11.1, NFR-11.2, NFR-11.3, NFR-12.1, NFR-12.2, NFR-12.3, NFR-13.1, NFR-13.2, NFR-14.1, NFR-14.2, NFR-14.3, NFR-15.1, NFR-16.1 and NFR-16.2. Each requires its own concrete implementation-level allowed/denied evidence. Unknown technical parameters retain a named prerequisite; they are never silently passed or weakened for a prototype.

### Clause-to-gate allocation

All FR-01.1–01.2, FR-02.1–02.8, FR-03.1–03.6, FR-04.1–04.3, FR-05.1–05.6, FR-06.1–06.3, FR-07.1–07.5 and FR-08.1–08.3 resolve to `product_functional_requirements`. NFR-01.1–01.4 resolve to `lifecycle_and_continuity`; NFR-02.1–02.3 resolve to `approved_visual_baseline_fidelity` and their explicit accessibility/browser checks. Security membership above is exhaustive for the current PRD. Every screen-map state also needs a functional simulation check. H1–H10 and representative-user checks are additional evidence classes, not substitutions for these obligations.

## Eval result format and evidence requirements

Each executed result records gate/check ID, exact evaluated revision or candidate/version, source/baseline hashes, executor, timestamp, route/state/viewport/content fixture, evidence kind/path/hash, observed result, findings and rerun rule. Store definition and execution status separately. Allowed execution statuses are not_run, passed, failed, blocked, deferred and not_applicable with a source-backed reason where applicable. Prepared means the check is specified, not that its runtime prerequisites exist or it has executed.

Synthetic Codex fixtures support process and failure-path evidence; the repository's MySQL 8.4 CI service can support grant encryption, restart, leadership and stale-write checks at a real database seam when its run result is recorded. Neither evidence class proves the GoDaddy secret's provenance, current subscription authorization, deployed schema/revision, a successful managed refresh on that host or a bounded live provider turn. Record those as distinct host/provider observations before a release claim.

Binding Status: approved_baseline; `nanoduck-electric-a-v8-20260914` is the actual owner-approved target. Approval does not execute any check. The orchestrator binds concrete QA IDs bidirectionally from the QA owner return; this later index is not a DoD-authoring prerequisite. At release, every applicable required gate and blocking finding must be closed with fresh evidence. Do not relabel an advisory failure as passed.

## Failure and blocker classification

Use the exact [PRD canonical severity and release-effect definition](prd.md#canonical-finding-severity-and-release-effect); this document does not maintain a competing shorthand scale. Every finding states applicability, source, evidence, severity, release effect and rationale. Missing runtime evidence blocks the corresponding release claim, not unrelated design exploration.

## PR, merge and completion rules

Commit/push reviewed artifacts to the already authorized public repository only after public-content and local checks. The existing artifact checker is limited to links/path hygiene/syntax; it does not prove the gates above. No production merge or deployment policy is invented. Architecture and this DoD are reconciled with the approved design and authorized browser-native implementation; QA and plan reconciliation follow before a release decision.

## Out of scope and open questions

No provider generation, deployment or data deletion is executed by this owner. Owner-confirmed Safari/Chrome Ukrainian probing and local implementation tests are narrowly scoped evidence, not release completion. Real provider/host evidence, grant reseed provenance and refresh behavior, technical parameter values and representative-user sessions remain named future prerequisites. Prototype comparison evidence is a later evaluation lookup; whole-design approval is recorded; production and representative-user evidence remain pending. These gate definitions do not claim that a prototype walkthrough passes production checks.
