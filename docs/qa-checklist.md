# QA checklist

## Source references and execution status

Current PRD, context/terms, guardrails, journey, screen map, wireframes, design brief and architecture/DoD supply the definitions below. **Formal checks prepared; indexed executions remain not_run. Release readiness: not_evaluated.** A separately recorded prototype walkthrough exists within the limited scope below; authoring this checklist does not execute the formal suite. Visual binding: `nanoduck-electric-a-v8-20260914`, the exact target/tree and permitted variance in the design brief. Runtime prerequisites and parameters named in architecture must be supplied before implementation execution. A prepared check does not imply those prerequisites already exist.

All checks are required/applicable unless an explicit exception is recorded. User group: the sole owner. Default viewport matrix: 320/390/430/768/1280/1440 CSS px; supported-browser release checks use Safari/Chrome/Firefox/Edge. Each check identifies its narrower scope. Findings, severity, release effect, recommendation, executor and actual evidence are unassessed until a run is recorded. Every failure uses the wireframe recovery sequence.

## Product acceptance

| Check | Source and use cases | Task / expected outcome | Gate and phase |
|---|---|---|---|
| QA-R01 | FR-01.1; UC-001 | Exercise the referenced use-case path and verify: Existing Google owner sign-in is the sole app-entry path; a returning owner reaches work without a connection checklist. | `product_functional_requirements` · implementation |
| QA-R02 | FR-01.2; UC-001 | Exercise the referenced use-case path and verify: Initial ordinary AI-processing consent is concise, explicit and reused until its scope changes. | `product_functional_requirements` · implementation |
| QA-R03 | FR-02.1; UC-002 | Exercise the referenced use-case path and verify: Text submission receives a distinguishable accepted state; an unsent draft never appears accepted. Enter submits in the chat composer and Shift+Enter retains a line break. | `product_functional_requirements` · implementation |
| QA-R04 | FR-02.2; UC-002 | Exercise the referenced use-case path and verify: The authenticated owner can attach a JPEG, PNG or WebP image at most 8 MiB; PDF, SVG, video, audio, archives and other types are rejected. | `product_functional_requirements` · implementation |
| QA-R05 | FR-02.3; UC-002 | Exercise the referenced use-case path and verify: Exercise fixed counts 1/2/3/5 and Auto 1–5 with synthetic owner requests. Verify Head, not a keyword selector, chooses the actual distinct permitted roles matching the count; Head and Critic are excluded. Reject duplicate, unknown or mismatched rosters. Keep Leadership/esoteric roles excluded and preserve the Spiritual Consultant and Psychotherapist boundaries. | `product_functional_requirements` · implementation |
| QA-R06 | FR-02.4; UC-002 | Exercise the referenced use-case path and verify: At actual provider and event seams, verify Head reads every ordered owner message/correction, saves a requested-output ledger, selects actual roles and commits each distinct complete Head task verbatim to only its named recipient. Test a long non-imperative task without XML, case-anchor, two-detail, character or generic fallback transformation. Every role receives the full owner text, confirmed discussion and direct source ledger; a text-only route states that image pixels are unavailable. Each specialist position, Critic challenge/reply, Head whole-team review decision, specialist final position, joint Critic review and single Head conclusion commits in order. Continue after Stop, provider failure and restart without duplicating confirmed messages; recover a legacy committed task-roster prefix exactly. A missing final position or Critic review blocks Head synthesis. Migrate exact old public Head-task, output-length and Critic defaults in encrypted documents and run snapshots while preserving owner edits and revision history. No direct Head-answer, specialist-to-specialist route or pre-conclusion Head advice appears. | `product_functional_requirements` · implementation |
| QA-R07 | FR-02.5; UC-002 | Exercise the referenced use-case path and verify: Give Critic a circular reply, irrelevant answer, fabricated buyer count, unsupported numeric claim and missing requested output. Verify a targeted stop-and-rework names the exact claim/omission, defect and required evidence, calculation or direct answer. The consultant materially revises, gives a specific grounded objection or acknowledges unavailable evidence; Head sees and arbitrates it. A sound position receives no invented defect. | `product_functional_requirements` · implementation |
| QA-R08 | FR-02.6; UC-002 | Exercise the referenced use-case path and verify: Consensus is declared only after Critic checks every requested output, all final positions and their evidence. Fluent agreement cannot conceal unsupported numbers or missing items. At the ceiling, unresolved issues produce explicitly provisional advice and a named evidence gap. | `product_functional_requirements` · implementation |
| QA-R09 | FR-02.7; UC-002 | Exercise the referenced use-case path and verify: Give Head a synthetic request with several distinct outputs and a final answer longer than the prior 2,000-character cap. Verify the complete saved/displayed/exported conclusion covers every output, cites direct available sources, names missing evidence and includes all necessary actions without a three-action cap, plus the main risk and revisit condition. | `product_functional_requirements` · implementation |
| QA-R10 | FR-02.8; UC-002 | Exercise the referenced use-case path and verify: The optional focused interview asks one question at a time, usually 1–3 and at most five, with a suggested answer or explicit assumption available. | `product_functional_requirements` · implementation |
| QA-R11 | FR-03.1; UC-002 | Exercise the referenced use-case path and verify: Full submitted business messages appear in canonical order with role, recipient where relevant and time; routine protocol paragraphs are omitted. During work, a labelled thought-bubble state and reachable Stop appear without an oversized conversation title; screen readers receive only newly arrived messages. The completed final synthesis creates the concise history/export title. | `product_functional_requirements` · implementation |
| QA-R12 | FR-03.2; UC-002 | Exercise the referenced use-case path and verify: Reject distinctive prohibited owner input and source metadata while allowing valid Ukrainian `які` and English descriptors “Russian” and “Belarusian”. For both provider adapters, mix safe prose with a prohibited sentence, including a URL and multiline case: omit the whole guilty sentence before persistence, retain safe remainder and a neutral marker, and log only kind/reason/count. Omit unsuitable Markdown/raw URLs from generated prose without promoting them to sources. A reply containing only prohibited prose, only an unsafe URL or only markers is unusable and gets exactly one same-role correction. A second unusable result saves a truthful System notice and offers Retry at the missing step; distinguish it from a truly empty provider response. Rejected drafts and System notices never enter subsequent business context. | `product_functional_requirements` · implementation |
| QA-R13 | FR-03.3; UC-003 | Exercise the referenced use-case path and verify: Owner context added during work is accepted into the same consultation without rewriting prior messages. | `product_functional_requirements` · implementation |
| QA-R14 | FR-03.4; UC-003 | Exercise the referenced use-case path and verify: Stop remains reachable during active work and prevents late work from becoming a newly visible result. | `product_functional_requirements` · implementation |
| QA-R15 | FR-03.5; UC-003 | Exercise the referenced use-case path and verify: Fail a specialist reply after a confirmed Critic challenge and fail a closing position or joint review. Retry must preserve the complete saved roster/message prefix, resume only the missing step, exclude System notices from provider context/step counts, then commit closing contributions and Head synthesis in order. Verify the same snapshot and generation fencing in memory/MySQL, denial while another run is active, stale conditional-update refusal, disabled repeat activation and useful feedback on refused recovery. | `product_functional_requirements` · implementation |
| QA-R16 | FR-03.6; UC-003 | Exercise the referenced use-case path and verify: New consultation creates a separate record while enforcing only one active consultation; start it above the chat and from Conversations, with no New item in either global menu. | `product_functional_requirements` · implementation |
| QA-R17 | FR-04.1; UC-007 | Exercise the referenced use-case path and verify: Use a synthetic request containing a private phone plus a separate safe public-topic need. Inspect the actual web-enabled prompt: it contains only Head’s non-web-generated, validated minimal public query and public instructions, never the private phone, full discussion or saved guidance. A numeric article ID in a safe public HTTPS URL remains valid. Unsafe query material is withheld with an explicit evidence limitation; no special owner research keyword is needed. | `product_functional_requirements` · implementation |
| QA-R18 | FR-04.2; UC-007 | Exercise the referenced use-case path and verify: Retain more than eight valid direct sources with title, URL, supported claim, retrieval time and publication date when available. Validate each source metadata record independently; reject forbidden language/hosts and private destinations without discarding unrelated valid sources. An unsuitable generated prose URL is omitted and never becomes a verified citation. | `product_functional_requirements` · implementation |
| QA-R19 | FR-04.3; UC-007 | Exercise the referenced use-case path and verify: Have a text-only Claude Critic identify a specific evidence gap. Head forms a new minimal public query without web access; a separate restricted Codex web step receives only that validated query and public instructions. The saved source becomes visible to later review without switching the Critic provider or granting it tools. | `product_functional_requirements` · implementation |
| QA-R20 | FR-05.1; UC-005 | Exercise the referenced use-case path and verify: Settings exposes functioning model and reasoning selectors separately for Head/specialists and Critic, including the optional Critic provider, using only the current supported catalog. The current values visibly read Head/specialists `gpt-6-astra` / `xhigh` and Critic Claude Code / Opus 5.5 / Medium (Default). With a ready Claude catalog, the provider remains visible and Opus 5.5 offers exactly Medium, High, Extra and Max, excluding Low. With Claude unavailable, show the same choice as a labelled preview but block its save and use without claiming entitlement. Reopen the app after a source update with old CSS/JavaScript cached; current HTML and changed asset URLs must show current controls without a manual cache clear. With a Codex catalog advertising `gpt-6-sol`, both the Head/specialist and Critic Codex model controls offer GPT-6 Sol and only their returned supported efforts; without that catalog evidence, neither control invents entitlement or silently substitutes it. Changing to a model that excludes the current effort requires a deliberate compatible effort choice. Switch the Critic provider away and back before choosing that effort; the unresolved value must remain invalid rather than silently selecting the first available effort. At the process seam, assert that the pinned Claude Code CLI is `2.1.280`, `claude-opus-5-5` / `medium` produces `--model claude-opus-5-5 --effort medium` without a moving alias, Extra produces `xhigh`, and a legacy `claude-opus-5` snapshot passes its exact model ID or fails visibly if unsupported. During an active Codex turn, read the current model catalog on that same app-server connection: a successful read permits only the newly advertised model/effort pairs for future settings. If the active-connection catalog read fails, show the temporary limit and retain only each exact saved Codex pair; permit unrelated preference saves, but reject a new or crossed pair at the server even if the browser submits it. Neither path starts another Codex child or treats a saved pair as fresh entitlement. | `product_functional_requirements` · implementation |
| QA-R21 | FR-05.2; UC-005 | Exercise the referenced use-case path and verify: Saved changes apply to future runs; each active run retains the exact provider/model/reasoning, specialist-count, discussion-depth and runtime-instructions Markdown/revision snapshot used at acceptance. Exercise empty, pre-Opus, immediately preceding and current-revision settings rows: the first two receive Opus 5.5 / Medium exactly once with unrelated preferences preserved; an immediately preceding Opus 5.5 / Low choice in the active or inactive Claude branch advances only that effort to Medium once; a later supported GPT-6 Sol save in either Codex branch survives repeated restart without changing the other branch; an inactive saved GPT-6 Sol Critic branch survives temporary catalog omission but cannot be activated until the pair is supported again; and an accepted pre-migration run snapshot remains unchanged. Repeat through configuration recovery. The independent message-sound preference applies to incoming browser feedback and never changes an active consultation’s logic. While a Codex turn is active, save an unrelated preference and, when a fresh catalog arrives through the active connection, a supported future model/effort; the accepted run must keep its original snapshot. Repeat with a failed catalog read: saving unchanged Codex pairs plus unrelated preferences succeeds, while a changed or crossed pair is refused without altering that run or its saved settings. | `product_functional_requirements` · implementation |
| QA-R22 | FR-05.3; UC-005 | Exercise the referenced use-case path and verify: Settings accepts specialist count 1/2/3/5/Auto and depth ceiling 1/3/5/Auto, with copy explaining Head’s decision. Every selected specialist receives at least one challenge and reply. Head may close after one productive whole-team round even at a higher ceiling, may direct another targeted round, and never exceeds the selected ceiling or Auto 10. Preserve roster/review decisions and confirmed messages across Stop/Retry and legacy prefixes; unresolved advice remains provisional. Verify the approved Knock/Chime/Ripple/Off controls, owner-activated preview, exact accepted Knock v5 bytes, versioned asset URL, HTTP WAV response, browser playback and visual feedback when audio is blocked. | `product_functional_requirements` · implementation |
| QA-R23 | FR-05.4; UC-005 | Exercise the referenced use-case path and verify: Usage presents real known quota/reset information or explicitly unavailable information; it never invents a per-session charge. | `product_functional_requirements` · implementation |
| QA-R24 | FR-05.5; UC-005 | Exercise the referenced use-case path and verify: Only a genuine authorization failure for the selected provider offers contextual reauthorization; quota and outage show their actual next action. Distinguish a genuinely missing, revoked or unusable selected Codex grant from temporary catalog, storage, leadership and provider outages; only the former gives a trusted reseed action, and an inactive Codex branch causes no warning. A malformed bootstrap seed must not displace an independently valid stored grant. | `product_functional_requirements` · implementation |
| QA-R60 | FR-05.6; UC-005 | Exercise the referenced use-case path and verify: An empty owned database receives exactly one validated document from the one-time deployment secret during migration, then can restart without that secret or a repository instruction file. Database export without the data key exposes no instruction plaintext. Settings loads the encrypted current document, rejects a missing required heading or placeholder without replacing its revision, accepts a valid edit, rejects a stale-browser save, lists saved-version metadata, retrieves a version only for the authenticated owner, and restores it by creating a fresh revision. Start a consultation, save or restore a later valid version, and verify the active run/provider still receives only the accepted Markdown/revision snapshot. Attempt to use document text to change authorization, role routing, finite resource ceilings or language/source restrictions; server invariants remain effective. | `product_functional_requirements` · implementation |
| QA-R25 | FR-06.1; UC-004 | Exercise the referenced use-case path and verify: Clicking and keyboard-activating the saved conversation row reopens complete confirmed conversation content, owned attachments and source records; Export and Delete do not open the record. Reload the record, Conversations and Settings surfaces and verify each stays in the same tab and restores its selected surface/reading position; Discussion also restores its selected local tab and record. Inspect tab storage to confirm it contains neither draft text nor conversation content and is cleared at Logoff. On the GoDaddy MySQL route, run the same read when the driver returns JSON columns as decoded settings, run snapshot and source values; the browser receives the canonical record without `service_unavailable`. | `product_functional_requirements` · implementation |
| QA-R26 | FR-06.2; UC-004 | Exercise the referenced use-case path and verify: Export returns a downloadable .rtf with application/rtf and no-store headers. Open it with a real rich-text reader and verify title, bold speaker/recipient names, all messages in canonical order, date/time with explicit browser time zone, paragraphs, supported Markdown emphasis/lists/headings, Ukrainian text and emoji, sources and saved image references. Confirm valid bold text has no raw Markdown delimiters. RTF control text from messages/titles must remain inert; no embedded executable objects/fields, drafts, credentials or runtime instructions appear. Test absent/invalid time zone (UTC/422), anonymous and deleted-record requests (401/404). Native-reader rendering is distinct from browser/API or full-release evidence. | `product_functional_requirements` · implementation |
| QA-R27 | FR-06.3; UC-004 | Exercise the referenced use-case path and verify: Explicit whole-conversation deletion removes its record and owned attachments; cancellation leaves them unchanged. | `product_functional_requirements` · implementation |
| QA-R28 | FR-07.1; UC-006 | Exercise the referenced use-case path and verify: In current Safari and Chrome, browser speech recognition begins only after explicit Start and required browser permission; active recognition and the recognition-service disclosure are visible. Verify Ukrainian `uk-UA` where browser language support is Ukrainian. | `product_functional_requirements` · implementation |
| QA-R29 | FR-07.2; UC-006 | Exercise the referenced use-case path and verify: Stop ends browser recognition and exposes editable text; Cancel aborts recognition. Verify that NanoDuck sends no voice audio or transcription request. | `product_functional_requirements` · implementation |
| QA-R30 | FR-07.3; UC-006 | Exercise the referenced use-case path and verify: The owner receives an editable transcript before any send, with no automatic submission. | `product_functional_requirements` · implementation |
| QA-R31 | FR-07.4; UC-006 | Exercise the referenced use-case path and verify: Permission, unavailable browser/service, language, network and interruption failures preserve the typed draft and offer retry or typing. | `product_functional_requirements` · implementation |
| QA-R32 | FR-07.5; UC-006 | Exercise the referenced use-case path and verify: Background recognition is prevented and the recognition session is stopped or aborted when it ends. | `product_functional_requirements` · implementation |
| QA-R33 | FR-08.1; UC-001, UC-003, UC-005 | Exercise the referenced use-case path and verify: Desktop navigation is a standard floating bar available while scrolling; mobile uses a labelled hamburger for Discussion, Conversations and Settings while a separate, directly visible bar button shows Login while anonymous or Logoff while authenticated (also before consent). Verify this button stays visible with the mobile menu closed at 320px and at the actual narrow in-app preview width; there is no duplicate dropdown action. Verify both entry and exit, labels after reload, disabled pending requests, retry feedback on failure, closed mobile menu after action and no crowded bar at its breakpoint. New remains above the chat and in Conversations. | `product_functional_requirements` · implementation |
| QA-R34 | FR-08.2; UC-005 | Exercise the referenced use-case path and verify: The preference destination is literally labelled Settings; the rejected slogan and Your space label are removed. | `product_functional_requirements` · implementation |
| QA-R35 | FR-08.3; UC-001, UC-002, UC-003, UC-004, UC-005, UC-006, UC-007 | Exercise the referenced use-case path and verify: The owner-requested combined black design uses Ember messages/local tabs and Cobalt chat/composer geometry, a recognizable icon-only SVG microphone, `You` for every visible owner label with an `I` avatar on owner messages, a visible filled Send button and the approved Electric palette after the three-palette comparison; all required flows remain available and Review states is absent from the normal view. | `product_functional_requirements` · both |
| QA-R36 | NFR-01.1; UC-003 | Exercise the referenced use-case path and verify: An acknowledged message survives browser refresh, network interruption and application restart. Reload an authenticated active discussion after scrolling and verify the same browser tab returns to its prior local tab, record and reading position with no stored draft or content. Simulate a slow ephemeral provider turn whose terminal notification has no message items; combine its matching completed message item with successful terminal completion and commit exactly one response without requesting saved history. Verify early notifications are retained, unrelated thread/turn events are ignored, and failed turns, cancelled waits or connection closure never commit partial output. Restart during an unresolved provider turn and verify the saved run resumes from its last confirmed message. Rotate the managed Codex credential during inspection and a bounded turn, then verify graceful turn completion, Stop, connection failure and provider shutdown commit the validated refresh before the private home is removed; a new process must reuse the refreshed encrypted row even when the unchanged deployment seed remains. Record the hard-kill-after-rotation window separately as a recovery limit, without claiming that application tests remove it. | `lifecycle_and_continuity` · implementation |
| QA-R37 | NFR-01.2; UC-003 | Exercise the referenced use-case path and verify: Reconnect/retry cannot duplicate a confirmed reply or allow concurrent runs to corrupt the canonical record. | `lifecycle_and_continuity` · implementation |
| QA-R38 | NFR-01.3; UC-003 | Exercise the referenced use-case path and verify: Verify 1/3/5/Auto depth is a ceiling, not a quota: all specialists receive one complete Critic challenge/reply; Head closes or continues only after a whole-team review. Auto permits at most 10 rounds per specialist; no filler rounds. Retain the ten-minute continuation boundary and explicit capacity errors. | `lifecycle_and_continuity` · implementation |
| QA-R39 | NFR-01.4; UC-003 | Exercise the referenced use-case path and verify: Measure the existing 5-second acknowledgment, 30-second first useful contribution and 60-second stalled-work visibility targets without presenting filler as useful output. | `lifecycle_and_continuity` · implementation |
| QA-R40 | NFR-02.1; UC-001, UC-002, UC-003, UC-004, UC-005, UC-006, UC-007 | Exercise the referenced use-case path and verify: Core flows meet the WCAG 2.2 AA planning target, including keyboard, visible/unobscured focus, semantic names/states, errors, contrast and relevant assistive technology interactions. | `approved_visual_baseline_fidelity` · implementation |
| QA-R41 | NFR-02.2; UC-001, UC-002, UC-003, UC-004, UC-005, UC-006, UC-007 | Exercise the referenced use-case path and verify: Core flows reflow from 320 CSS px without page-level horizontal scroll and remain usable at 200% text size; genuine two-dimensional content retains an accessible way to inspect it. | `approved_visual_baseline_fidelity` · implementation |
| QA-R42 | NFR-02.3; UC-001, UC-006 | Exercise the referenced use-case path and verify: Release evidence covers current Safari, Chrome, Firefox and Edge on relevant mobile/desktop devices; an unavailable microphone has the FR-07.4 fallback. | `approved_visual_baseline_fidelity` · implementation |
| QA-R43 | NFR-10.1; UC-001 | Exercise the referenced use-case path and verify: Replay a valid callback and supply wrong issuer/audience/subject/signature; only the valid owner path establishes access. | `product_security_requirements` · implementation |
| QA-R44 | NFR-10.2; UC-001, UC-005 | Exercise the referenced use-case path and verify: Sign in and verify access just before 24 hours, after inactivity and browser reopening; expire at 24 hours. Verify sign-out/revocation invalidates access immediately, activity cannot extend absolute expiry, and another identity cannot revoke owner sessions. | `product_security_requirements` · implementation |
| QA-R45 | NFR-10.3; UC-001, UC-002, UC-003, UC-004, UC-005, UC-006 | Exercise the referenced use-case path and verify: Attempt each private read/mutation anonymously, as a non-owner and with forged object/field identifiers; no protected content or state changes. | `product_security_requirements` · implementation |
| QA-R46 | NFR-11.1; UC-002, UC-004, UC-005, UC-006, UC-007 | Exercise the referenced use-case path and verify: allowed Markdown emphasis, lists, quotations, inline code and approved HTTPS links render as content. Submit executable markup, unsafe URLs, malformed envelopes, prohibited Russian/Belarusian language and injection strings through text/files/provider output; they remain inert or are rejected. | `product_security_requirements` · implementation |
| QA-R47 | NFR-11.2; UC-002, UC-003, UC-005, UC-006, UC-007 | Exercise the referenced use-case path and verify: Repeat acceptance, race Stop with late responses and exceed declared resource limits; accepted history stays atomic and bounded. | `product_security_requirements` · implementation |
| QA-R48 | NFR-11.3; UC-001, UC-002, UC-003, UC-004, UC-005, UC-006 | Exercise the referenced use-case path and verify: Forge cross-origin writes and proxy headers; verify cookie/header/origin policy and denied changes. Fetch the HTML and each same-origin CSS/JavaScript asset, including imported modules: every URL hash matches its response bytes, stale or wrong hashes do not return different bytes, and CSP/MIME/nosniff remain correct. | `product_security_requirements` · implementation |
| QA-R49 | NFR-12.1; UC-007 | Exercise the referenced use-case path and verify: Fetch public, private/metadata, redirecting and prohibited `.ru`/`.by`/`.su` or Cyrillic-equivalent targets; only permitted English/Ukrainian public evidence is returned, with no secret disclosure. | `product_security_requirements` · implementation |
| QA-R50 | NFR-12.2; UC-002, UC-004, UC-006 | Exercise the referenced use-case path and verify: Upload valid JPEG/PNG/WebP, client-MIME mismatch, malformed/truncated signature, over-8-MiB, PDF, SVG, video, audio and archive fixtures. Only valid bounded images persist as encrypted opaque blobs; their owner-only retrieval uses fixed validated type, attachment disposition and `nosniff`. Every rejection retains the typed draft and exposes no server path. Verify browser voice creates no NanoDuck upload. | `product_security_requirements` · implementation |
| QA-R51 | NFR-12.3; UC-002, UC-005, UC-007 | Exercise the referenced use-case path and verify: Inject requests for secrets, tools, paid fallback and external messages; no permission or provider boundary expands. For Claude Critic, assert a command-level denial of known tools, no direct research flag, and a text-only role prompt containing the current owner question and prior confirmed discussion. Return a CLI `result` containing `<invoke name="Bash">…</invoke>`; verify one natural-language retry and that neither attempt can enter events, sources or the browser. A Codex grant or write-back failure must not switch to a paid API key, another model or another provider. | `product_security_requirements` · implementation |
| QA-R52 | NFR-13.1; UC-001, UC-004, UC-005 | Exercise the referenced use-case path and verify: Read database ciphertext without keys, alter it and restore with authorized separated keys; plaintext stays protected and tampering fails. Include the owned encrypted Codex grant row: database-only inspection and the owner recovery export reveal no usable grant, a changed authentication tag or wrong separated key denies use, and isolated restoration with the authorized data key recovers an intact row. Verify the grant-specific key is distinct from ordinary record encryption. | `product_security_requirements` · implementation |
| QA-R53 | NFR-13.2; UC-001, UC-002, UC-004, UC-005, UC-006, UC-007 | Exercise the referenced use-case path and verify: Use valid/invalid service certificates and privileged/unrelated credentials; unsafe transfer fails while allowed service identity works. | `product_security_requirements` · implementation |
| QA-R54 | NFR-14.1; UC-002, UC-004, UC-006, UC-007 | Exercise the referenced use-case path and verify: Inspect provider/search requests, URLs, caches and logs with marked private fixtures; only authorized minimized content leaves the boundary. Inspect browser assets, API responses, URLs, owner recovery export and protected logs for a marked synthetic Codex seed and rotated grant; neither credential nor a usable derivative may escape those boundaries. | `product_security_requirements` · implementation |
| QA-R55 | NFR-14.2; UC-001, UC-004, UC-006 | Exercise the referenced use-case path and verify: Cancel/fail/background voice recognition and sign out offline; browser recognition ends, client content is cleared as required and browser history cannot reveal private data. HTML and private API responses remain no-store, while public versioned assets contain no private response or draft. NanoDuck receives no audio. | `product_security_requirements` · implementation |
| QA-R56 | NFR-14.3; UC-003, UC-004 | Exercise the referenced use-case path and verify: Delete one conversation, restore an isolated backup and inspect unrelated data; accepted records recover and deletion decisions remain applied. | `product_security_requirements` · implementation |
| QA-R57 | NFR-15.1; UC-001, UC-002, UC-004 | Exercise the referenced use-case path and verify: Inspect exact deployment/dependencies/configuration and forbidden paths; no private/debug/repository artifact is served and only the named app changes. Before upload, verify root `package.json` has non-empty name/version/main, a valid SemVer version, existing start entry point, non-empty build/start scripts, `process.env.PORT`, `0.0.0.0` server binding, every runtime import in `dependencies`, and no tracked `node_modules`. On GoDaddy, verify `NANODUCK_RUNTIME_MODE=production` overrides a host-owned development `NODE_ENV`; `npm start` applies the idempotent owned `nanoduck_*` migration before serving without accessing tables outside the owned application schema; the settings revision may create or update only the owner row in `nanoduck_settings`, never reads or writes `nanoduck_runs`, and a second start performs no preference update, injected `DB_*` forms a certificate-verified connection, the pinned Codex package starts from the deployed dependency tree, the installed Claude Code package matches the lockfile pin `2.1.280` and a bounded allowed turn actually uses `claude-opus-5-5` / `medium`, and exact 32-byte data/recovery keys accept canonical Base64URL, padded standard Base64, hexadecimal, literal 32-byte input, or only their exact same-name `.env` envelope without derivation or alteration; exactly one configured Codex auth source (`CODEX_APP_SERVER_AUTH_B64` or `CODEX_APP_SERVER_AUTH_GZIP_B64` on this host) becomes `auth.json` only inside a removed-after-use private app-server home. Verify the running Preview health response, configured `PORT`, effective production binding and deployed source revision before treating a dashboard port-warning tooltip as a process failure. Treat the configured Codex secret as a bootstrap seed for exactly one owned encrypted nanoduck_provider_grants row, never as a per-request copy: first migration seeds it, unchanged seed cannot roll back a refreshed row after restart, and a fresh trusted login can deliberately reseed it. Verify file-backed Codex auth in the private 0600 home, write-back before cleanup, database-backed leadership across idle periods, and exclusive preflight while the app owns that grant. Record the MySQL 8.4 CI integration result separately from a scoped GoDaddy Preview and Published inspection of deployed revision, owned schema, secret-name presence and selected-provider behavior; neither synthetic fixtures nor a healthy build prove the live subscription or refresh. | `product_security_requirements` · implementation |
| QA-R58 | NFR-16.1; UC-001, UC-002, UC-003, UC-004, UC-005, UC-006, UC-007 | Exercise the referenced use-case path and verify: Produce auth/validation/recovery events and log-injection strings; protected metadata remains useful without leaking or forging content. Malformed-seed and grant-persistence failures log only fixed or normalized categories with useful operation context; no auth bytes, refresh token, seed fingerprint, raw provider diagnostic or private path reaches protected logs. | `product_security_requirements` · implementation |
| QA-R59 | NFR-16.2; UC-001, UC-002, UC-003, UC-004, UC-005, UC-006, UC-007 | Exercise the referenced use-case path and verify: Fail validation/provider/research/storage/logging paths then retry; access stays denied where required and invalid partial commits never appear. Inject a provider JSON-RPC error and verify that protected logs retain only its normalized category, safe numeric code and failed operation; the browser receives the matching recovery state and never receives raw provider diagnostics, prompts or authentication material. Return two Claude tool transcripts and verify the run fails safely with the owner question retained, no raw tool material committed and no partial Head synthesis. At the grant seam, fail read, decryption, validation, refresh write-back and database leadership, and race a stale generation: deny provider success, retain the prior durable row, preserve the accepted question and commit no partial answer. Test bounded persistence retry when recovery is possible; after an unrecoverable rotated-token write-back failure or hard kill, require a fresh trusted reseed rather than claiming automatic recovery. No failure permits paid fallback or model substitution. | `product_security_requirements` · implementation |

## Screen and state checks

For historical comparison, run each candidate independently using fictional data. For implementation acceptance, run this same state inventory against the approved Electric baseline and the actual implemented application; simulated state switching cannot pass runtime obligations. The separate inspection URL (`?inspect=1`) exposes state/locale fixtures without contacting live services. The normal preview and intended app omit Review states; verify New occupies its former workspace position and is absent from desktop/mobile menus. Historical simulated checks do not pass production clauses. Implementation executions use actual controls and services, plus the corresponding clause/security checks. QA-S01–QA-S09 and QA-H01–QA-H10 apply to both prototype and implementation; retain their results separately.

| Check | Source states and task | Expected evidence |
|---|---|---|
| QA-S01 | ST-01, ST-02, ST-03, ST-04; S-01; UC-001. Walk Sign in: signed-out, consent, denied, expired. Verify all controls, safe exits, preserved draft/record and observable completion from the wireframe contract. | Actual state/interaction observations, candidate/version/source hash, viewport and console result; no simulated-runtime claim. |
| QA-S02 | ST-05, ST-06, ST-07, ST-08, ST-09, ST-10, ST-11, ST-12, ST-13, ST-14, ST-15, ST-16, ST-17; S-02; UC-002, UC-003. Walk Discussion and composer: empty, draft, attachment-error, clarification, active, complete, stopped, offline, provider-auth, quota, research-error, system-error, long-content. Verify all controls, safe exits, preserved draft/record and observable completion from the wireframe contract. | Actual state/interaction observations, candidate/version/source hash, viewport and console result; no simulated-runtime claim. |
| QA-S03 | ST-18, ST-19, ST-20; S-03; UC-004. Walk Conversations: populated, empty, unavailable. Verify all controls, safe exits, preserved draft/record and observable completion from the wireframe contract. | Actual state/interaction observations, candidate/version/source hash, viewport and console result; no simulated-runtime claim. |
| QA-S04 | ST-21, ST-22, ST-23, ST-24, ST-25; S-04; UC-005. Walk Settings after an app update without clearing old cached assets: visible Claude Code Critic branch, Opus 5.5 / Medium (Default) with High/Extra/Max and no Low, labelled unavailable preview with Save disabled, GPT-6 Sol selection with a compatible effort in each Codex control, reload persistence, invalid-settings (model combination or runtime instructions), selected-provider catalog-unavailable, inactive-provider unavailable and session-control. Verify all controls, safe exits, preserved saved selection/draft/record and observable completion from the wireframe contract; no unavailable state silently switches provider/model/effort. Include Settings during an active Codex turn: a fresh catalog from that same process permits a supported future pair, while a failed catalog read leaves exact saved pairs and unrelated Save available with a clear temporary limitation. Confirm a changed or crossed pair is visibly refused by the server and the accepted run retains its snapshot. | Actual state/interaction observations, candidate/version/source hash, viewport and console result; no simulated-runtime claim. |
| QA-S05 | ST-26, ST-27, ST-28, ST-29, ST-30, ST-31, ST-32, ST-33; S-05; UC-006. Walk Voice input: ready, recognizing, recognition-complete, transcript, permission-denied, microphone-unavailable, interrupted, recognition-error. Verify all controls, safe exits, preserved draft/record and observable completion from the wireframe contract. | Actual state/interaction observations, candidate/version/source hash, viewport and console result; no simulated-runtime claim. |
| QA-S06 | ST-34, ST-35, ST-36; S-06; UC-007, UC-004. Walk Sources and detail: list, detail, unavailable. Verify all controls, safe exits, preserved draft/record and observable completion from the wireframe contract. | Actual state/interaction observations, candidate/version/source hash, viewport and console result; no simulated-runtime claim. |
| QA-S07 | ST-37, ST-38; S-07; UC-002, UC-004. Walk Outcome: recommendation, provisional. Verify all controls, safe exits, preserved draft/record and observable completion from the wireframe contract. | Actual state/interaction observations, candidate/version/source hash, viewport and console result; no simulated-runtime claim. |
| QA-S08 | ST-39, ST-40, ST-41, ST-42; S-08; UC-004. Walk Record action confirmation: delete-confirmation, deleted, export-ready, export-error. Verify all controls, safe exits, preserved draft/record and observable completion from the wireframe contract. | Actual state/interaction observations, candidate/version/source hash, viewport and console result; no simulated-runtime claim. |
| QA-S09 | ST-43, ST-44; S-09; UC-001, UC-003, UC-005. Walk Mobile navigation: closed, open. Verify all controls, safe exits, preserved draft/record and observable completion from the wireframe contract. | Actual state/interaction observations, candidate/version/source hash, viewport and console result; no simulated-runtime claim. |

## Heuristic usability checks

Expert review is separate from visual captures and representative-user testing. All H1–H10 apply; there are no excluded heuristics. Each check runs across relevant default and recovery states on desktop and mobile, with the primary owner tasks from the journey.

| Check | Heuristic/task | Scope and expected evidence |
|---|---|---|
| QA-H01 | H1: Identify draft, accepted, active, paused, browser recognition, transcript and save/delete feedback without misleading completion. | J-01–J-07; all relevant surfaces/UCs. Named reviewer, candidate scope, observations and classified findings. |
| QA-H02 | H2: Use literal Settings, meaningful role/claim/action labels and readable English/Ukrainian fixture content; reject transport jargon and vague slogans. | J-01–J-07; all relevant surfaces/UCs. Named reviewer, candidate scope, observations and classified findings. |
| QA-H03 | H3: Complete Stop, Continue, Cancel, transcript editing, close/Back and cancelled deletion without loss or traps. | J-01–J-07; all relevant surfaces/UCs. Named reviewer, candidate scope, observations and classified findings. |
| QA-H04 | H4: Compare desktop floating navigation and mobile hamburger, fields and focus order across the approved Electric baseline and the implementation; retain earlier three-candidate comparisons as historical evidence. | J-01–J-07; all relevant surfaces/UCs. Named reviewer, candidate scope, observations and classified findings. |
| QA-H05 | H5: Attempt invalid model/effort pairs, unconsented entry, repeat Send and voice/delete cancellation; prevent unintended actions. During an active Codex turn, attempt a crossed saved pair and a newly unsupported effort after catalog failure; the server rejects both without losing unrelated edits. | J-01–J-07; all relevant surfaces/UCs. Named reviewer, candidate scope, observations and classified findings. |
| QA-H06 | H6: Return to a conversation and identify its context, saved/current model values and supporting sources without recall. | J-01–J-07; all relevant surfaces/UCs. Named reviewer, candidate scope, observations and classified findings. |
| QA-H07 | H7: Complete the simple novice path and repeated work with visible mic/navigation and keyboard alternatives; no unnecessary setup. | J-01–J-07; all relevant surfaces/UCs. Named reviewer, candidate scope, observations and classified findings. |
| QA-H08 | H8: Inspect first viewport and long content; hierarchy emphasizes conversation and controls without clipped or decorative noise. | J-01–J-07; all relevant surfaces/UCs. Named reviewer, candidate scope, observations and classified findings. |
| QA-H09 | H9: Trigger every recovery family and identify cause, preserved work, next action, retry/cancel and success. Distinguish selected-provider reauthorization from quota, transient catalog and grant-storage failures; preserve the current run and give only the applicable next action. | J-01–J-07; all relevant surfaces/UCs. Named reviewer, candidate scope, observations and classified findings. |
| QA-H10 | H10: Find concise processing/voice/invalid-choice/source help at the point it is needed; no documentation dump. | J-01–J-07; all relevant surfaces/UCs. Named reviewer, candidate scope, observations and classified findings. |

## Usability validation checks

Not conducted. The owner approved the design without a recorded representative-user task session; the design brief records the pre-approval deferral risk and post-implementation release obligation. No AI agent is a representative participant.

| Check | Owner task and trace | Success and timing |
|---|---|---|
| QA-U01 | Dictate and correct a Ukrainian request without accidental sending; UC-006 | Safari and Chrome browser recognition through editable transcript; no moderator rescue, accidental send or lost draft. Phone and desktop; required before release. Actual participant/session observation required. |
| QA-U02 | Identify the Critic objection, consultant response and source; UC-002, UC-007 | Correctly locate and explain evidence/uncertainty without moderator rescue. Phone and desktop; pre-approval when feasible, required after implementation. Actual participant/session observation required. |
| QA-U03 | Change future model/reasoning without changing the active run; UC-005 | Return after an app update and find the current Claude Code Critic option without clearing the cache; observe the migrated Opus 5.5 / Medium preference and its Medium/High/Extra/Max choices, save GPT-6 Sol with a compatible effort in a Codex control, reload, and correctly identify which future consultation uses each value while an accepted run retains its snapshot. Phone and desktop; pre-approval when feasible, required after implementation. Actual participant/session observation required. While a Codex turn is active, save a future supported choice after a live catalog read and an unrelated preference when the catalog is temporarily limited; recognize which run keeps the old tuple and why a new pair cannot be saved without fresh catalog evidence. |
| QA-U04 | Stop/recover/export and cancel a deletion; UC-003, UC-004 | Complete actions with no lost accepted work, duplication or unintended deletion. Phone and desktop; pre-approval when feasible, required after implementation. Actual participant/session observation required. |

## UX/UI checks and evidence requirements

The checks mapped to NFR-02.1, NFR-02.2 and NFR-02.3 must be checked against their actual source IDs, not assumed from ordinal numbers. For the selected Electric revision inspect the NanoDuck Consulting Group name, supplied outline-duck SVG in navigation/sign-in/favicon, light outline on dark surfaces and original charcoal-outline export, full mobile wordmark and its accessible navigation label. Inspect HappyPro blue controls/large heading, raspberry Critic, the warmer Head yellow and distinct Spiritual Consultant/Psychotherapist role tokens. Check every gradient stop against its actual heading surface, white Send text against blue, readable small blue accents, avatar initials and forced-colors fallback. Retain checks for an icon-only SVG microphone with accessible name, `You` role header and `I` avatar for owner entries, New placement, agent identity without colour alone and Ember message/tab formatting. For all UI runs include 320px reflow, 200% text resizing, visible/unobscured focus, native select labels/states, dialog/menu keyboard behavior, primary touch targets, color contrast, reduced motion, forced colors and long English/Ukrainian content. Record device/browser and separate assistive-technology limitations. Do not infer WCAG conformance from screenshots.

Each run records check ID, candidate/version or implementation revision, source/target hashes, executor, time, fixture/state/route/viewport, evidence kind/path/hash, result and findings with P0–P3 severity and blocking/advisory rationale. Baseline fidelity requires the approved Electric A v8 target and permitted variance in the design brief. Security evidence must come from the actual trusted boundary, not hidden/disabled controls.

## Execution evidence and history

The separately authorized v1 candidate walkthrough is recorded in the [verification report](verification.md) and the [H1–H10 review](../forge/design/evidence/heuristic-review.json). The three visual records cover all 44 rendered states and selected interactions: [Cobalt](../forge/design/evidence/a-v1-visual-qa.json), [Ember](../forge/design/evidence/b-v1-visual-qa.json), [Prism](../forge/design/evidence/c-v1-visual-qa.json). These are partial supporting observations for QA-S01–QA-S09 and QA-H01–QA-H10, not full executions of their broader every-control, accessibility and browser obligations. None of the 83 indexed checks is promoted to passed by this reconciliation. Runtime checks remain not_run. The rejected initial prototype has separate historical evidence and does not verify these three candidates.

A later candidate version may reuse only specifically applicable observations when exact render-tree equality is verified and recorded; retain the original executor, time and scope. New URL/browser observations receive new receipts. The clarified explicit-Critic acceptance in QA-R05 requires later actual-provider evidence and is not proved by a scripted conversation.

## Evidence limits, release readiness and open questions

Release readiness: not_evaluated. Production authentication, provider execution, durability, security, cross-device operation, deployment and restore remain unevaluated. Browser-native voice is implemented locally and has owner-confirmed desktop Safari/Chrome Ukrainian probing; mobile Safari and Android Chrome still require formal runs. Architecture must resolve live Opus 5.5 entitlement, the installed CLI version and exact-model execution, an actual bounded provider turn, hosted settings migration, host behavior and technical parameters before affected release runs. Representative-user and assistive-technology validation remain required evidence, not implied success. Design approval and the Phase 3 implementation prompt are recorded.
