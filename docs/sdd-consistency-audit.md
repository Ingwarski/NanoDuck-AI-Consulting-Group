# SDD consistency audit

## Current Opus 5.5 Critic preference — 22 September 2026

The owner supplied a current Claude picker and directed Critic to Claude Code Opus 5.5 with Medium effort. The repository now stores `claude-opus-5-5` / `medium`, labels Medium as the default, routes only Critic through Claude and keeps Head/specialists on Codex `gpt-6-astra` / `xhigh`. A one-time settings revision updates future preferences while preserving unrelated settings and every accepted run snapshot. Later owner changes remain authoritative.

The isolated command maps the new owner-facing model to the Claude CLI's `opus` alias and passes `medium` unchanged. A legacy Opus 5 snapshot retains its exact ID instead of silently using the moving alias. Local provider, validation, routing and migration tests cover these boundaries. This correction does not claim a live Claude grant, paid entitlement, provider turn, hosted database migration, deployment or release readiness. The Electric A v8 visual baseline is unchanged.

## Current accepted Knock sound — 18 September 2026

R-23 promotes the exact v5 sample accepted by the owner: “this is it.” The sound blends the sharper v3 and woody v4 impacts with quiet early reflections, keeping three taps exactly 250 ms apart. The 45 ms direct impact and tiny echo end within 92 ms per tap. The new versioned path, `/sounds/table-taps-250ms-v5.wav`, is shared by Settings Preview and incoming-message alerts and prevents reuse of the cached v3 file. Chime/Ripple and the existing sound setting are unchanged.

The [owner acceptance](../forge/runs/U-03/r23-accepted-knock-20260918/owner-acceptance.json) binds the accepted file to the identical application bytes. Earlier exploration receipts retain their original pre-acceptance status as history. The [verification receipt](../forge/runs/U-03/r23-accepted-knock-20260918/verification.json) records 129 passing tests, HTTP WAV delivery identical to the accepted file, exact 250 ms PCM timing without clipping, and successful local Chrome playback from Settings. The architecture, QA and U-03 plan now describe the accepted blended sound with a tiny echo; the outdated glass-tap description is removed. The DoD definitions remain valid and their architecture binding is refreshed. All four affected owner checks, the SDD audit and implementation check passed.

This is a repository/local correction. No GoDaddy deployment, live provider, production database operation, new Safari test or full release evaluation is claimed.

## Superseded dry table-tap alert — 18 September 2026

R-22 replaces the rejected Nokia-based Knock with the owner-refined CC0 ping-pong-ball-on-table sample. The owner accepted v2’s bright, dry character and requested slightly more emphasis. V3 keeps its 40 ms impact, pitch, EQ, decay and exactly 250 ms onset-to-onset spacing, with mild compression and a peak of 0.96. Measured RMS level rises approximately 3.03 dB, with no clipped samples and silence between impacts. Settings Preview and incoming alerts use the same bundled WAV and playback path. Chime/Ripple and all other settings remain unchanged. Earlier exploratory audio files and source receipts are preserved.

The [R-22 receipt](../forge/runs/U-03/r22-table-tap-20260918/verification.json) records 129 passing tests, HTTP media delivery and independent PCM checks. Owner listening established the v2 character; v3 is the requested emphasis refinement. There is no claim of a new hosted deployment, physical-device audibility check or full cross-browser release verification. Architecture, QA and U-03 planning use the current 250 ms dry-tap contract and are reconciled through their owners.

## Current Ukrainian validation and failed-run recovery correction — 18 September 2026

R-20 follows a read-only inspection of the latest hosted discussion: it stopped on a language-policy failure after the third Critic challenge, before the next specialist reply, and offered no continuation control. Rejected drafts are deliberately ephemeral, so the exact triggering text in that hosted draft is unavailable. A local regression reproduces a concrete validator defect: the common Ukrainian word “які” was in the prohibited-language blacklist. Removing that ambiguous entry accepts valid Ukrainian in owner input, both provider adapters and source metadata while preserving distinctive prohibited-language markers and forbidden hosts.

Failed runs now expose Retry and use the existing owner-authorized continuation route in both stores. Recovery preserves the accepted snapshot, serializes with other active work and advances the generation. Saved System notices remain visible but are excluded from provider context and coordinator step matching. The missing role resumes without replay, and every required specialist final position and Critic closing review still precedes Head synthesis. No schema, model, role, provider, permission or approved visual-baseline change is introduced.

The [R-20 receipt](../forge/runs/U-04/r20-ukrainian-policy-recovery-20260918/verification.json) records 129 passing tests, including the previously failing Ukrainian acceptance and interrupted-third-exchange cases, both adapters, HTTP retry and mocked MySQL transaction checks. Native Chrome inspection of an isolated local workspace confirms visible Retry, preserved messages, a restarted attempt and accepted Ukrainian input. The browser provider was deliberately unavailable; successful completion is fixture-backed coordinator evidence. Hosted repair verification, actual MySQL concurrency and full cross-browser release checks remain separate. SDD owner/source bindings and the coupled context invocation are reconciled in dependency order.

## Superseded quick-knock sound correction — 18 September 2026

R-19 replaces the synthesized low-frequency Knock with an edited Nokia N73 Message 2 recording from the owner-supplied archive URL. Three recorded taps now start 140 ms apart instead of approximately 280 ms, with original pitch preserved, entry silence removed and no clipped samples. This is explicitly an edited version, not an unmodified original. Settings Preview and incoming-message alerts share the same bundled same-origin PCM WAV; Chime/Ripple retain generated local WAVs. The server serves WAV as `audio/wav`, so playback needs no third-party request or additional browser permission.

The [R-19 receipt](../forge/runs/U-03/r19-quick-knock-20260918/verification.json) records 119 passing tests, correct HTTP media delivery, measured tap timing and successful Chrome preview playback. Owner listening and physical Safari/Chrome audibility remain separate from the browser accepting playback. The bundled Python 3.12 runtime also permits the formal SDD checker: R-19 repairs stale R16 dispatch pointers left behind by R18 and restores one shared context-bundle invocation, preserving existing document decisions and history. No GoDaddy, provider or database operation is included.

## Current Claude Critic context and output correction — 18 September 2026

R-18 corrects two defects in the Claude Code Critic adapter. Unlike the Codex path, it previously passed only the short Critic assignment and omitted the owner question, prior confirmed discussion, role/output contract and policy. The Critic therefore could not see the specialist position it was expected to challenge. It also accepted a Claude Code internal `<invoke>` tool transcript as a natural-language Critic message. The corrected adapter reconstructs the same consultation prompt context as Codex, while Claude Critic has direct research disabled. Its isolated command uses a documented tool denial list, `dontAsk`, a one-turn cap and a text-only system prompt. A tool invocation/transcript is rejected before persistence, retried once as text-only, and then fails safely without exposing the trace. A coordinator-level guard prevents any provider adapter from committing a trace if an adapter regresses. Existing saved conversations are untouched; the malformed test conversation remains owner-controlled history.

Focused local tests confirm the full Critic context reaches the Claude invocation, direct research is disabled, tool denial is present, a first trace is replaced, a second trace never reaches the event stream and cannot lead to Head synthesis. The locally installed Claude package lacks its native binary, so this is adapter and coordinator evidence rather than an actual Claude command or hosted consultation. The [R-18 receipt](../forge/runs/U-04/r18-claude-critic-context-20260918/verification.json) records the final scope and evidence. GoDaddy deployment, hosted database changes and release gates remain separate.

## Current discussion-feedback correction — 18 September 2026

R-17 keeps a browser refresh in the same tab and returns an authenticated owner to the current app surface and reading position. On Discussion it reloads the protected record, restores Discussion/Outcome/Sources and then scrolls to the prior point. The short-lived tab record contains only page, local tab, record identifier and scroll offset; it contains no draft or conversation content, never appears in the URL and is removed at Logoff. A deleted/unavailable record safely falls back to the normal Discussion surface. Unit checks cover state validation and the local HTTP check confirms the served client module. Local Chrome verification signed in to the isolated fixture, scrolled Settings near its bottom, refreshed the same tab, and observed Settings at the same rendered position. The [R-17 local receipt](../forge/runs/U-07/r17-same-tab-refresh-20260918/verification.json) records the exact scope and the still-unrun host, Safari and release checks.

R-16 removes the oversized in-progress conversation heading. While the team works, Discussion shows a compact, labelled thought bubble and reachable Stop; after final synthesis, the saved record receives a deterministic concise decision title. Settings adds owner-selected browser-local incoming-message sounds: Knock (tuk-tuk-tuk), Chime, Ripple and Off, with an explicit preview and a visual equivalent that remains when browser audio is blocked.

The current interaction correction makes the saved conversation summary itself an accessible Open control, leaving Export and Delete as distinct actions. Enter now submits the chat composer while Shift+Enter adds a line break. Before R-19, the sound implementation used generated local WAV media through an HTML audio element, and the restrictive media policy explicitly permits its `blob:` source; this replaced the inaudible Web Audio oscillator path. Local Chrome automation accepted the preview playback request, but audibility still needs a physical Safari and Chrome release check. At R-16, Claude Code Settings reflected the then-current owner-supplied picker: Opus 5 with Low, Medium, High (default), Extra and Max. That historical preference is superseded by the Opus 5.5 / Medium revision recorded above; legacy accepted snapshots keep their exact model and effort.

The latest failed consultation exposed a control-flow error, not an allowed-policy exception: the provider correctly withheld a draft containing prohibited language or source material, then the coordinator treated that one withheld draft as terminal. R-16 re-invokes the exact same role/task once with a server-owned language/source correction. Neither rejected draft nor its source data is stored, rendered or announced. A second failure follows the existing recoverable-failure path.

The [R-16 verification receipt](../forge/runs/U-04/r16-discussion-feedback-20260918/verification.json) records 109 passing local tests, the controlled policy-replacement fixture, a Chrome local walkthrough and 390px mobile check. It is historical evidence for the former Web Audio path and does not certify current WAV audibility. The [SDD audit](../forge/sdd-manifest.json) passes all thirteen source-bound artifacts without issues or warnings. GoDaddy deployment and release gates remain separate.

## Current rich-text export correction — 17 September 2026

R-15 replaces the normal raw JSON download with a readable RTF document. The existing Export action produces bold speaker/recipient names, explicit local timestamps, formatted paragraphs, emphasis, lists and source references in saved-message order. Image references are retained; image binaries are not embedded. The server escapes all text before serialization and preserves the existing authentication, no-store and deletion boundaries. No package, database schema or extra format-selection screen is added.

The [local verification](../forge/runs/U-07/r15-rtf-export-20260917/verification.json) records 105 passing checks, an actual Chrome download with all thirteen saved speakers, native macOS RTF parsing, and [visual LibreOffice rendering](../forge/runs/U-07/r15-rtf-export-20260917/formatting-example.png) of a [fictional formatting example](../forge/runs/U-07/r15-rtf-export-20260917/formatting-example.rtf). TextEdit GUI opening was not completed; that limitation and font-dependent emoji rendering are recorded without claiming a pass. All thirteen source-bound artifacts pass the [SDD audit](../forge/runs/U-07/r15-rtf-export-20260917/sdd-audit.json). The Electric A v8 baseline, security obligations, models and consultation behavior remain unchanged. GoDaddy publication and full release gates remain separate.

## Previous visible menu-button correction — 17 September 2026

R-14 corrects R-13 placement: the session button must be directly on the bar at all widths. The actual narrow in-app preview had hidden it inside the mobile dropdown. One persistent button now shows Login or Logoff beside the Menu trigger; no duplicate action remains in the dropdown. The [local verification](../forge/runs/U-03/r14-visible-login-menu-20260917/verification.json) records both label/action transitions in the owner-visible preview and Chrome at 320 CSS pixels. All 101 checks and the [13-artifact SDD audit](../forge/runs/U-03/r14-visible-login-menu-20260917/sdd-audit.json) pass. Frozen A v8 files and server authentication remain unchanged. The local preview is refreshed; GoDaddy deployment remains separate.

## Previous menu implementation — 17 September 2026

The owner requested Login in the global menu, replaced by Logoff when authenticated. R-13 adds the action to desktop and mobile navigation and reuses existing authentication/session routes, including consent-pending sessions. The [local verification receipt](../forge/runs/U-03/r13-login-menu-20260917/verification.json) records Chrome desktop/mobile interaction, logout invalidation and 101 passing checks. All 13 artifact bindings pass the [SDD audit](../forge/runs/U-03/r13-login-menu-20260917/sdd-audit.json); affected owner decisions are reconciled, unchanged security obligations and frozen A v8 files remain intact. This change is not deployed to Preview or Published.

## Current Published startup verification — 17 September 2026

The R-12 missing-secret startup failure is resolved. After the owner's manual configuration/publication, the GoDaddy **Published** tab shows `1a2832e` with infrastructure and site OK. Direct HTTPS checks return the NanoDuck homepage with HTTP 200 and `/healthz` with `status: alive`, `store: mysql`. A fresh Chrome owner sign-in completes the Google callback on the Published origin and reaches the processing-consent screen. The optional instruction-bootstrap sync was skipped because the owned database is already initialized. The owner consent checkbox remains unchecked, so a fresh Published consultation and history/instruction reads are not claimed. The [Published receipt](../forge/runs/U-08/r12-published-config-20260917/published-verification.json) records these limits. Full release gates remain unevaluated.

### Earlier diagnosis and remediation

The owner accepted the current Preview and reported a failed Published deployment. The supplied startup log fails at `config.mjs:139`: Published lacks `DATA_ENCRYPTION_KEY`. An isolated local config call reproduces that exact missing-variable error, and the authenticated GoDaddy secret-name inventory confirms seven Preview names are absent from Published. This is environment configuration drift; no application-code change or weakened encryption validation is justified. Preview remains healthy at `7b1e2d4`; that does not establish Published readiness.

The [R-12 diagnosis](../forge/runs/U-08/r12-published-config-20260917/verification.json) records the missing names and the prepared native Preview-to-Published synchronization. The owner's subsequent sync attempt failed with `Failed to sync secrets`; a reload confirmed the same seven names were absent. The [follow-up receipt](../forge/runs/U-08/r12-published-config-20260917/sync-failure.json) records the platform error, confirmed manual-import option and metadata-only API limitation. The internal sync-error cause remains unknown. The earlier deployment record located the original private Preview setup file outside Git. A private Published import was prepared with the exact same four secret values, the Published `APP_ORIGIN` and production runtime mode; the already-consumed instruction bootstrap was omitted. Local parsing validated its encoding, original-value preservation and owner-only file permissions before handoff. The later successful Published observations above supersede that import blocker. No hosted secret value was exposed through the agent, no database was reset, and no unrelated app was touched. These operational receipts supplement the earlier Preview-only boundary without changing product behavior, SDD ownership, approved design or the dependency graph.

## Current consolidation correction — 16 September 2026

R-11 addresses the owner's report that Head concluded before a consolidated team response. R-10 proved per-specialist debate coverage, not final consolidation. Each selected specialist now submits a final position addressed to Head; Critic reviews those positions together and addresses its closing assessment to Head; only then may Head produce **Consolidated advice**. Auto continues within its existing ten-exchange-per-specialist cap when Critic still objects. Fixed depth and the cap allow an explicitly provisional conclusion with unresolved disagreement. Closing contributions do not count as extra debate exchanges.

The new behavioral sections are added once to the encrypted, owner-editable runtime document as a reviewable version. Existing owner text, saved versions and completed conversations remain intact. Runtime sequencing is server-enforced. Head's final invocation has research disabled and is instructed to summarize the reviewed closing material without introducing a fresh position. The heading is stored with the final message; Outcome excludes task assignments and earlier questions' conclusions.

The [R-11 local receipt](../forge/runs/U-04/r11-consolidation-20260916/verification.json) covers all fixed team/depth combinations, delayed/failed closing messages, replay at every closing step, Auto closing disagreement/cap, unknown agreement after restart, instruction upgrade/edit preservation, authenticated HTTP/export and a Chrome fixture walkthrough. The full local suite passes 101 tests. SDD owners reconcile the changed behavior and preserve the approved Electric A v8 baseline, dependency graph and existing security obligations.

On 17 September, private Preview `49242bd` became healthy and a fresh actual-provider consultation with two specialists and depth 1 completed the full thirteen-message sequence: both directed debate pairs, Strategy's final position to Head, Finance's final position to Head, Critic's combined closing assessment to Head, and only then **Consolidated advice**. Outcome remained empty until that closure completed. Reload and reopening restored the sequence, heading and completed state. Settings showed the three added sections in a new encrypted-document migration revision with earlier versions retained; the original one-specialist preference and both Astra/xhigh settings were restored and confirmed. The [R-11 hosted receipt](../forge/runs/U-04/r11-consolidation-20260916/hosted-verification.json) records this limited result. Hosted Auto/failure cases and full release gates remain unevaluated.

## Current runtime-instructions state — 15 September 2026

The owner instruction document exists only as encrypted current and version records in the application database. There is no checked-in instruction document or runtime baseline. A fresh owned database can receive one validated document through exactly one one-time deployment secret during the idempotent production-start migration: plain `RUNTIME_INSTRUCTIONS_BOOTSTRAP_B64` or length-bounded `RUNTIME_INSTRUCTIONS_BOOTSTRAP_GZIP_B64`; remove that secret after bootstrapping is independently verified. Settings exposes the current document, metadata-only saved-version list, owner-only read-only review, and explicit restore that creates a fresh revision. Current and historical document text uses the existing data-encryption key; metadata remains separate. Deterministic tests cover encryption, optimistic stale-save protection, history and restore. On the hosted Preview, MySQL now contains one encrypted instruction record; its ciphertext and metadata were observed without plaintext. The R-09 private-Preview check below proves provider completion but is insufficient for complete team review: Finance never received a Critic challenge. Owner Settings review, deletion of the bootstrap secret and the full release gates remain separate verification work.

R-01 removes the direct Head-answer route from the current implementation. On the next owned application start, a legacy current `Direct Head Answer` section is replaced by `Consultation Routing` in a new encrypted `routing_migration` revision; prior encrypted versions remain reviewable, and restoring a prior version reapplies the non-direct routing constraint. A resumed legacy run snapshot is converted before a prompt can be rendered, so it cannot revive the removed route. The R-09 hosted check below reached final synthesis but skipped Finance review; it cannot pass full consultation acceptance. It does not independently reverify every encrypted routing-migration and restore case.

## Current whole-team review correction — 16 September 2026

The owner identified the missing Critic reply to Finance. The cause was a global exchange counter: with two specialists and depth 1, only Strategy received a challenge/reply before Head synthesized. Earlier fixture expectations repeated that mistake. The R-09 hosted receipt remains unchanged as historical transport and persistence evidence; its completed state is not proof of a complete review.

R-10 makes depth apply to every selected specialist. Auto assesses agreement after the whole team, with a cap of 10 exchanges per specialist. Saved role/recipient pairs are reused on resume; legacy global agreement cannot waive missing coverage, and missing agreement metadata is treated as unknown. Completed historical records remain preserved. Product intent, PRD, the coupled context/terms bundle, journey, wireframes, architecture, DoD, QA and plan are reconciled through their owners; unchanged guardrails, screen inventory and approved Electric A v8 remain revalidated. The security review retains all existing obligations: no new actor, data type, integration or privilege; bounded provider use and Stop remain enforced.

The [local verification receipt](../forge/runs/U-04/r10-whole-team-review-20260916/verification.json) records reproduction on the old code, passing fixed count/depth coverage, Auto disagreement/cap, resume, failed Finance review and authenticated HTTP checks. Private Preview `fb5ec12` is healthy. A fresh actual-provider run with two specialists and depth 1 completed both Strategy and Finance Critic challenge/reply pairs before Head synthesized. Finance revised its evaluation after Critic challenged the short test's ability to distinguish incremental profit from normal variation; the final Outcome retained that qualification. Reloading and reopening Conversations restored all ten messages and the completed state. The original one-specialist preference was restored, and both Astra/xhigh settings were preserved. The [hosted receipt](../forge/runs/U-04/r10-whole-team-review-20260916/hosted-verification.json) records this limited result; full release gates remain unevaluated.

## Current GoDaddy host-compatibility state — 16 September 2026

Fresh Chrome inspection on 16 September confirms that the named Personal AI Consulting Group private Preview is healthy at `9a13a51`, with both infrastructure and site status OK. Preview startup logs show the owned schema migration and a listening server. The earlier R-08 claim that a fresh Preview rollback proved an encryption-key failure was incorrect: the inspected rollback detail belonged to an older Published attempt. Same-name `.env` envelope support is a tested parser compatibility option, not an established cause of the current incident; no secret value was inspected, replaced or logged.

A fresh synthetic consultation on this healthy Preview fails at `thread/read` with `rpc_-32600`. A separate local probe of the same pinned Codex 0.153.1, without credentials or a model call, reproduces the exact reason: ephemeral threads reject `includeTurns`. R-09 replaces that unsupported history poll with correlated completed-item and terminal-turn events. Early notifications are retained; unrelated events and partial deltas cannot become accepted output. Stop, connection closure and the existing 540000 ms deadline release an unresolved invocation. The updated architecture, DoD, QA and plan describe this protocol consistently; R-05 polling evidence remains historical and cannot certify this repair. The local regression suite passes. Private Preview `c8bbd1c` then deployed with infrastructure/site status OK. A synthetic bakery consultation completed through two Head tasks, two specialist positions, one Critic challenge, the addressed specialist revision and one final Head synthesis. All seven provider calls completed via notifications. The Outcome rendered, and a browser reload followed by reopening Conversations restored all eight owner/agent messages and the completed state. The run took about five minutes; the final synthesis alone took 161851 ms, so this observation is not a performance-release pass. The [hosted verification receipt](../forge/runs/U-04/r09-ephemeral-provider-events-20260916/hosted-verification.json) records the exact commit and limited scope.

The existing Preview configuration and owned encrypted database are retained. Node 22, the production-mode override, injected certificate-verified `DB_*`, protected auth provisioning and the named app boundary remain as recorded in [deployment-boundary.md](deployment-boundary.md). No Published release or unrelated GoDaddy resource is part of this repair. Fresh Google OAuth, mobile/browser voice, recovery and the full release gates remain separate verification work.

## Prior current-state snapshot — historical before Preview activation

Current state: Electric A v8 remains the approved visual baseline. The former Fast/Balanced/Thorough/Ultra pace allocation is historical. The current reconciliation replaces it with independent specialist-count (1/2/3/5/Auto) and discussion-depth (1/3/5/Auto) settings, including Head selection of an Auto team and a 10-exchange Auto-depth ceiling. The manifest binds current hashes, the preserved frozen baseline and the renewed Phase 3 prompt receipt. The prior [approved-plan audit receipt](../forge/design/evidence/electric-v8-approved-plan-audit-20260914.json) remains historical evidence for its earlier source set. The owner authorized local Phase 3 work; browser-native voice, owner-only JPEG/PNG/WebP image attachments at most 8 MiB, role/language/source enforcement and the two consultation controls are implemented locally. The [14 September U-08 receipt](../forge/runs/U-08/phase3-local-integration-preparation-20260914/verification.json) and its [non-deploying cutover package](../forge/runs/U-08/phase3-local-integration-preparation-20260914/cutover-package.md) remain historical: their 82-check count and source hashes describe that earlier source set. The current [fixture-topology receipt](../forge/runs/U-08/phase3-local-fixture-topology-20260915/verification.json) records the fresh local memory-store/fake-provider workflow, required recipient order, and Discussion/Outcome/Sources rendering. It does not treat the fixture prose or in-app voice observation as provider, Ukrainian-voice, device, production or release evidence. The 83 formal checks and six release gates remain prepared / not_run and release readiness remains not_evaluated; the source switch is the only GoDaddy operation performed. U-02 has a limited [session-and-data receipt](../forge/runs/U-02/phase3-local-session-data-20260915/verification.json): unauthenticated access is denied, the local cookie is fixed at 86400 seconds, consent does not extend expiry, expired signed cookies fail, and encryption/MySQL seams pass controlled tests. It does not prove deployed Google OAuth or GoDaddy MySQL. U-04 has source-bound [recipient-topology](../forge/runs/U-04/phase3-recipient-topology-correction-20260914/verification.json), [Head-task enforcement](../forge/runs/U-04/phase3-head-task-enforcement-20260915/verification.json), [response-quality](../forge/runs/U-04/phase3-response-quality-20260915/verification.json) and [database-document](../forge/runs/U-04/phase3-runtime-instructions-database-20260915/verification.json) receipts. At the time of the historical U-04 receipt, the source was one validated Markdown document in the existing application database; its checked-in text was an insert-once seed and never a live baseline or override. The current database-only instruction implementation is stated above. The local deterministic and browser evidence covers seed preservation, valid/invalid/stale saves and immutable accepted run snapshots; it does not prove hosted MySQL durability or a deployed provider. Their other deterministic and actual local provider evidence shows task-only Head handoffs, specialist-to-Critic positions, Critic-specialist revisions and final Head synthesis, with role-specific message bounds. U-05 has a limited [actual-research receipt](../forge/runs/U-05/phase3-local-research-source-20260915/verification.json): an official English source passed through title, direct HTTPS URL, claim and times without prohibited language or host. Broader provider and release compliance remain unobserved; neither U-02, U-05 nor U-08 treats formal acceptance as passed. U-03 has a limited [safe-Markdown rendering receipt](../forge/runs/U-03/phase3-safe-markdown-rendering-20260915/verification.json): the browser client parses a restricted Markdown subset into constructed DOM nodes, raw markup/unsafe links stay inert or fail validation, focused tests pass and the existing local app preview reloads normally. It does not replace formal QA-R46 or release acceptance.

The pre-approval logo v8 audit remains historical in [its original receipt](../forge/design/evidence/nanoduck-v8-sdd-audit.json).

The prior Electric v4 audit remains preserved in [its own receipt](../forge/design/evidence/electric-v4-sdd-audit.json).

The earlier combined v3 audit is preserved in [its own receipt](../forge/design/evidence/hybrid-v3-sdd-audit.json).

## Earlier v2 audit snapshot

13 September 2026. Review of the corrected design-stage documents; this report records observations and does not own product requirements, architecture or approval.

The 12 canonical pre-design documents pass the installed pipeline audit. Their owners, required-before edges, invocation/output identities and consumed-source hashes match the machine contract. The context and canonical-terms pair remains one invocation with two outputs. All 30 reachable installed skill/reference resources resolve. The pipeline skill repository remains clean and unchanged.

### Earlier corrections

| Issue | Owning correction |
|---|---|
| Product brief linked the rejected green prototype as the new candidate. | Product-idea owner labels it historical and points to the current review inventory as a later lookup. |
| Product evidence was read without current source bindings. | Product-idea owner declares review, model and deployment evidence plus rejected design material; orchestrator records exact hashes. |
| Direct Head-answer route conflicted with the owner’s Head-only task/final-synthesis rule. | Product, PRD, architecture, plan and QA now require specialist-and-Critic discussion for every accepted question; R-01 removes the branch and migrates the current encrypted contract to `Consultation Routing`. |
| Source revision labels and future-generation wording were stale. | Product-idea, PRD and context owners replace them with current or durable ownership wording. |
| Screen map gave architecture ownership of user-facing routes. | Screen-map owner retains those routes; architecture owns API/runtime paths and hosting. |
| Canonical surface locations were described as identical candidate hashes. | Screen-map owner records observed v1 fragment/overlay differences without changing required states or frozen files. |
| Design brief described completed v1 freeze and heuristic review as future work. | Design owner records the existing evidence and retains the proposed, unapproved baseline. |
| DoD referenced an absent PRD severity glossary and condensed it inconsistently. | PRD owner records the shared P0–P3 scale; DoD references that definition. |
| QA/DoD status mixed prepared formal checks with prototype observations. | Owners separate the 82 unexecuted definitions from limited v1 observations; runtime prerequisites apply before execution. |
| Repository guidance and a historical verification link targeted the wrong design generation. | Repository guidance points to active versioned candidates; the rejected design links its own historical evidence. |
| Design execution used a non-contract state value. | Orchestrator uses the prescribed state values and records the actual browser blocker separately. |

Revalidation followed the required-before order. Stable JOB, UC, requirement, state, gate and check IDs were preserved; the 17 security obligations and pinned ASVS assessment were not weakened. Later references did not become creation dependencies. The original v1 candidate files, input snapshots and receipts remain preserved.

### Earlier verification and boundary

- Canonical `sdd_check.py --audit`: **passed**, no issues or warnings.
- Before/after owner checks: **passed** after current return metadata and the QA index were integrated.
- Ownership, dependency, consumption and installed-link audit: **passed**.
- Local artifact checks: **passed**; exactly three active candidates, with historical versions retained.
- v2 render reuse: **exact byte equality** with v1 for all nine HTML/CSS/JavaScript files. New v2 routes and Settings were checked in the Codex in-app browser; the comparison embeds the three current routes.
- Formal QA: **82 prepared / not_run**. No production security, representative-user or release pass is claimed.
- `prototype-candidates` after-check: **blocked only by `browser_receipt`**. The Mac is locked, so the required separate visible Safari review of v2 has not occurred. Successful v1 Safari receipts were not relabelled.

The preserved v2 operational evidence is in [the audit receipt](../forge/design/evidence/sdd-consistency-audit-20260913.json), [verification](verification.md) and [the manifest](../forge/sdd-manifest.json). The Mac was subsequently unlocked; the user requested a new combined revision, and current v3 has its own successful Safari evidence. V2 was superseded without rewriting its blocked receipts. Whole-design approval, post-approval reconciliation, planning and a later explicit implementation prompt remain separate steps. No GoDaddy operation was performed.
